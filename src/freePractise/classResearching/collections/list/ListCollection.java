package freePractise.classResearching.collections.list;

import static helpers.Helpers.*;

public class ListCollection {
    public static void listCollection() {
        printArticle("List interface - ordered collection (sequence)");

        /*
        * Список - коллекция элементов, которые расположены в некоторой последовательности друг за другом
        * Каждый элемент списка имеет индекс. Список позволяет иметь элементы с одинаковыми значениями.
        * */

//        ArrayListCollection.arrayListCollectoin();
//        LinkedListCollection.linkedListCollection();

        printArticleEnding();
    }
}
